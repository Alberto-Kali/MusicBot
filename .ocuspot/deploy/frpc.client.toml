# Generated by ocuspot.
# Example frpc snippet for carrier "backend-kvm".
# The managed deploy path renders the final frpc config from control-plane metadata.

serverAddr = "__OCUSPOT_GATEWAY_HOST__"
serverPort = __OCUSPOT_FRP_BIND_PORT__
auth.method = "token"
auth.token = "${OCUSPOT_FRP_PCONF_BUS}"

[[proxies]]
name = "music-bot"
type = "http"
localPort = 3123
subdomain = "music"
